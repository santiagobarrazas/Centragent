{
  "name": "@centragent/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate --schema prisma/schema.prisma",
    "db:push": "prisma db push --schema prisma/schema.prisma --accept-data-loss",
    "db:constraints": "prisma db execute --file prisma/sql/constraints.sql --schema prisma/schema.prisma",
    "db:seed": "tsx src/seed.ts",
    "db:setup": "prisma db push --schema prisma/schema.prisma --accept-data-loss && prisma db execute --file prisma/sql/constraints.sql --schema prisma/schema.prisma && tsx src/seed.ts",
    "db:studio": "prisma studio --schema prisma/schema.prisma",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^6.10.1",
    "@centragent/shared": "workspace:*",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "prisma": "^6.10.1",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
